# Direct dependencies only. Pinned to the versions the service was validated on.
# torch/torchvision come in as ultralytics dependencies; install the CUDA build
# from https://pytorch.org first if you want GPU inference.
flask==3.0.3
ultralytics==8.2.102
opencv-python==4.10.0.84
numpy==1.26.4
requests==2.32.3

# tests
pytest==8.3.3
